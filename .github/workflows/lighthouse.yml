name: Lighthouse

concurrency:
  group: lighthouse-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on: [pull_request]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Wait for Vercel preview deployment
        uses: patrickedqvist/wait-for-vercel-preview@v1.3.3
        id: waitForVercelPreviewDeployment
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          max_timeout: 600
          check_interval: 15
          vercel_protection_bypass_header: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}

      - name: Lighthouse
        uses: foo-software/lighthouse-check-action@v12.0.1
        with:
          urls: ${{ steps.waitForVercelPreviewDeployment.outputs.url }}
          gitHubAccessToken: ${{ secrets.GITHUB_TOKEN }}
          extraHeaders: '{"x-vercel-protection-bypass":"${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}"}'
          device: "all"
          locale: en
          prCommentEnabled: true
